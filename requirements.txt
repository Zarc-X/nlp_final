transformers==4.57.3
torch==2.8.0
gradio==4.44.1
accelerate==1.10.1
bitsandbytes==0.48.2  # 用于量化加速（可选，仅GPU需要）
openai>=1.0.0  # 用于DashScope API调用（兼容OpenAI接口）
requests==2.32.5  # 用于其他HTTP请求
datasets==4.4.1  # 用于模型微调（可选）